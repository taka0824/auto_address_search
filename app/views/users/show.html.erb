

  <div class="container">
    <div class="row">
      <div class="col-xs-3">
        <%= render "users/sidebar", user: @user, book: Book.new %>
      </div>
      <div class="col-xs-9">
        <div>
          <h2>Books</h2>
            <table class="table table-hover">
              <thead>
                <tr>
                    <th></th>
                    <th>Title</th>
                    <th>Opinion</th>
                </tr>
              </thead>
              <% @user.books.each do |book| %>
                <tr>
                  <td><%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format:'jpeg', class:"img.profile_image pull-left profile_img" %></td>
                  <td><%= link_to "#{book.title}", book_path(book.id) %></td>
                  <td><%= book.body %></td>
                </tr>
              <% end %>
            </table>
        </div>
      </div>
    </div>
  </div>